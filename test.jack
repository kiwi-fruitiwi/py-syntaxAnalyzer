method void dispose () {
    var int i, j;

    if (false) {
        let s = "string constant";
        let s = null;
    }
    else { // There is no else keyword in the Square files.
        let x = this;
    }
    return;
}

{
    var SquareGame game;
    let game = game;
    do game.run();
    do game.dispose();
    return;
}


field int size, height, width, clowns;
let y = 0;
let str = "string constant test";
let number_1 = 2751;
let expr = scaleFactor;
let a[5] = 7;
let a[i] = Keyboard.readInt("ENTER THE NEXT NUMBER: ");
var SquareGame game;
var int x, y; // screen location of the square's top-left corner
